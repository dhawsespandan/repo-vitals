{
  "_meta": {
    "note": "SCAFFOLD — replace this entire file with the actual output of: python manage.py build_corpus --seed 42 --target 1000 --config corpus_frame.yaml",
    "command": "python manage.py build_corpus --seed 42 --target 1000 --config corpus_frame.yaml",
    "run_from": "backend/",
    "expected_fields": ["seed", "target", "admitted", "ecosystem_split", "strata", "timestamp", "config_hash", "dedup_discards", "admission_rate"]
  },
  "seed": 42,
  "target": 1000,
  "timestamp": "FILL_IN_AFTER_RUN",
  "config_hash": "FILL_IN_AFTER_RUN",
  "admitted": "FILL_IN_AFTER_RUN (expected 900–1100)",
  "ecosystem_split": {
    "npm": "FILL_IN_AFTER_RUN (expected ~500)",
    "pypi": "FILL_IN_AFTER_RUN (expected ~500)"
  },
  "admission_rate_pct": "FILL_IN_AFTER_RUN (expected 40–60%)",
  "dedup_discards": "FILL_IN_AFTER_RUN (expected > 0)",
  "strata": {
    "npm_active": "FILL_IN_AFTER_RUN (must be > 0)",
    "npm_moderate": "FILL_IN_AFTER_RUN (must be > 0)",
    "npm_stale": "FILL_IN_AFTER_RUN (must be > 0 — critical for research)",
    "npm_very_stale": "FILL_IN_AFTER_RUN (must be > 0 — critical for research)",
    "pypi_active": "FILL_IN_AFTER_RUN (must be > 0)",
    "pypi_moderate": "FILL_IN_AFTER_RUN (must be > 0)",
    "pypi_stale": "FILL_IN_AFTER_RUN (must be > 0 — critical for research)",
    "pypi_very_stale": "FILL_IN_AFTER_RUN (must be > 0 — critical for research)"
  },
  "repos": [
    "FILL_IN_AFTER_RUN — array of admitted repo objects generated by the command"
  ]
}
