[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
django = ">=2.2,<3.0"
requests = { version = ">=2.31", extras = ["security"] }
widget = { git = "https://github.com/example/widget.git" }
shared = { path = "../shared" }

[dev-packages]
flask = "*"

[requires]
python_version = "3.11"
